server:
  port: ${SERVER_PORT:8080}

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/gmmx}
    username: ${DB_USERNAME:gmmx}
    password: ${DB_PASSWORD:gmmx123}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  jackson:
    time-zone: Asia/Kolkata

app:
  otp:
    expiry-minutes: 5
  cors-origins: ${CORS_ORIGINS:http://localhost:3000,http://localhost:8081}
